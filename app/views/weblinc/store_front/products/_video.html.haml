- if product.video_header.present? || product.video_embed.present?
  .video-plugin
    - if product.video_header.present?
      %h3.video-plugin__header= product.video_header
    - if product.video_embed.present?
      .video-plugin__responsive-frame{ style: product.frame_styles }!= product.video_embed
